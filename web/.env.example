# Browser-visible API base — used by client-side (React Query) fetches.
NEXT_PUBLIC_API_URL=http://localhost:4000

# Server-side API base — used by SSR / server components to reach the API
# directly (can differ from the public URL in containerized deploys).
API_URL=http://localhost:4000
