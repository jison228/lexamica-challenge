{
  "name": "lexamica-challenge",
  "version": "0.1.0",
  "private": true,
  "description": "Lexamica take-home — referral invitation lifecycle (NestJS API + Next.js web)",
  "scripts": {
    "install:all": "npm install && npm --prefix api install && npm --prefix web install",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "seed": "npm --prefix api run seed",
    "dev": "concurrently -n api,web -c blue,magenta \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm --prefix api run start:dev",
    "dev:web": "npm --prefix web run dev",
    "build": "npm --prefix api run build && npm --prefix web run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
